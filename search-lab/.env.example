# Supabase (서버 전용 - 절대 클라이언트에 노출 금지)
SUPABASE_URL=https://rvbzpimzgpdkcsopfuca.supabase.co
SUPABASE_SERVICE_ROLE_KEY=Project_Settings_API_의_service_role_secret_key

# 선택: 임베딩 모델 캐시 경로.
# 비워두면 vector-pipeline 이 이미 받아둔 캐시를 재사용하고,
# 그것도 없으면 <repo>/.model-cache 에 새로 내려받는다.
# MODEL_CACHE_DIR=
